const isDigit = /\d/.test;
const isLetter = /\w/.test;
const isOperator = /\+|-|\*|\/|\^/.test;

module.exports = s => {
  const tokens = [];

  // for (const i = 0; i < s.length; i++) {
  //   const c = s[i];

  //   if ()
  // }

  return tokens;
}
